@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewData["Title"] = "Dashboard - Tổng quan";
}

<div class="row">
    <!-- Thống kê Sản phẩm -->
    <div class="col-md-4">
        <div class="card text-white bg-primary mb-3 shadow">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h5 class="card-title">Sản phẩm</h5>
                        <h2 class="fw-bold">@ViewBag.TotalProducts</h2>
                    </div>
                    <i class="fas fa-box fa-3x opacity-50"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Thống kê Đơn hàng -->
    <div class="col-md-4">
        <div class="card text-white bg-success mb-3 shadow">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h5 class="card-title">Đơn hàng</h5>
                        <h2 class="fw-bold">@ViewBag.TotalOrders</h2>
                    </div>
                    <i class="fas fa-shopping-cart fa-3x opacity-50"></i>
                </div>
            </div>
        </div>
    </div>

    <!-- Thống kê Doanh thu -->
    <div class="col-md-4">
        <div class="card text-white bg-warning mb-3 shadow">
            <div class="card-body">
                <div class="d-flex justify-content-between align-items-center">
                    <div>
                        <h5 class="card-title text-dark">Doanh thu</h5>
                        <h2 class="fw-bold text-dark">@ViewBag.TotalRevenue.ToString("#,##0") đ</h2>
                    </div>
                    <i class="fas fa-money-bill-wave fa-3x text-dark opacity-50"></i>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="alert alert-info mt-4">
    <h4><i class="fas fa-info-circle me-2"></i>Hướng dẫn</h4>
    <p>Chào mừng Admin! Để quản lý dữ liệu, vui lòng chọn menu bên trái:</p>
    <ul>
        <li><strong>Quản lý Sản phẩm:</strong> Để thêm, sửa, xóa điện thoại, laptop...</li>
        <li><strong>Quản lý Đơn hàng:</strong> Để xem ai vừa đặt mua gì.</li>
    </ul>
</div>